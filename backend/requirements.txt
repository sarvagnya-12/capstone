fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic-settings==2.15.0
sqlalchemy==2.0.52
alembic==1.19.1
psycopg[binary]==3.3.4
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (unmaintained since 2020) is incompatible with bcrypt>=4.1's
# stricter 72-byte validation during its own internal backend self-test; pin
# to the last compatible bcrypt release.
bcrypt==4.0.1
python-jose[cryptography]==3.5.0
email-validator==2.3.0
python-multipart==0.0.32
pillow==12.3.0
pytest==9.1.1
httpx==0.28.1
pytest-cov==7.0.0

# GAN engine (Step 15+). CUDA build -- install with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# A plain `pip install -r requirements.txt` will NOT find these +cu126 builds
# on PyPI; run the command above first (or once more after this file), then
# `pip install -r requirements.txt` again to pick up everything else.
torch==2.13.0+cu126
torchvision==0.28.0+cu126
torchmetrics==1.9.0
requests==2.34.2
torch-fidelity==0.4.0

# LLM persona engine (Step 20+)
anthropic==0.122.0

# Sentiment analysis (Step 23+)
transformers==5.15.0

# Report export (Step 31+) -- pure-Python PDF generation, no external
# binary/system dependency (rules out weasyprint's need for a system
# Pango/Cairo install), simpler for deployment.
reportlab==5.0.0

# GAN training from scratch (Step 17). FastGAN via lightweight-gan replaced
# StyleGAN2 transfer learning after three measured failures; see
# app/ml/gan/train_fastgan.py. pytorch-fid backs the package's own FID metric.
lightweight-gan==1.2.1
pytorch-fid==0.3.0
