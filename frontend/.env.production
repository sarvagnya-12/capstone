# Empty on purpose: in the containerized build, nginx (see Dockerfile/nginx.conf)
# proxies /api to the backend service, so the frontend calls itself
# (same-origin relative paths) rather than a separate host:port -- no CORS,
# no hardcoded backend hostname needed at build time.
VITE_API_BASE_URL=
